============
Installation
============

At the command line::

    $ easy_install dj-model-history

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-model-history
    $ pip install dj-model-history
